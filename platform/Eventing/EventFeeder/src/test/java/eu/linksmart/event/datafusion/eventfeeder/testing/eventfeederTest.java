package eu.linksmart.event.datafusion.eventfeeder.testing;

import org.apache.log4j.Logger;
import org.junit.Test;

import static org.junit.Assert.assertTrue;

public class eventfeederTest {

    private Logger mlogger = Logger.getLogger(eventfeederTest.class.getName());

    @Test
    public void testSomething() {
        //TODO dummy assertion. Add your proper tests
        assertTrue(true);

    }
}